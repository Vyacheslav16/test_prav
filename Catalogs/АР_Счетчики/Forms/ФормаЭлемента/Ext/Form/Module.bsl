
////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ - ОБРАБОТЧИКИ СОБЫТИЙ РЕКВИЗИТОВ ШАПКИ

&НаКлиенте
Процедура НаименованиеНачалоВыбораИзСписка(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	
	// Формирование списка выбора.
	Список = Новый СписокЗначений();
	Если ЗначениеЗаполнено(Объект.МаркаСчетчика) Тогда
		Список.Добавить(Объект.МаркаСчетчика);
	КонецЕсли;
	Если ЗначениеЗаполнено(Объект.НомерСчетчика) Тогда
		Список.Добавить(Объект.НомерСчетчика);
		Если ЗначениеЗаполнено(Объект.МаркаСчетчика) Тогда
			Список.Добавить(Объект.МаркаСчетчика + "/" + Объект.НомерСчетчика);
		КонецЕсли;
	КонецЕсли;
	
	// Выбор из списка и обработка выбора.
	РезультатВыбора = ВыбратьИзСписка(Список, Элементы.Наименование);
	
	Если РезультатВыбора <> Неопределено Тогда
		Объект.Наименование = РезультатВыбора.Значение;
	КонецЕсли;
КонецПроцедуры


&НаКлиенте
Процедура ВидСчетчикаПриИзменении(Элемент)
	//Если ОбщийСчетчик(Объект.ВидСчетчика) Тогда //Объект.ВидСчетчика = Перечисления.АР_ВидыСчетчиков.Общий Тогда
	//	Элементы.Помещение.Заголовок = "Здание"
	//Иначе	
	//	Элементы.Помещение.Заголовок = "Помещение"
	//КонецЕсли;	
КонецПроцедуры

Функция ОбщийСчетчик(ВидСчетчика)
	Если ВидСчетчика = Перечисления.АР_ВидыСчетчиков.Общий Тогда
		Возврат Истина;
	Иначе
		Возврат Ложь;
	КонецЕсли;	
КонецФункции		

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Если ОбщийСчетчик(Объект.ВидСчетчика) Тогда //Объект.ВидСчетчика = Перечисления.АР_ВидыСчетчиков.Общий Тогда
	//	Элементы.Помещение.Заголовок = "Здание"
	//Иначе	
	//	Элементы.Помещение.Заголовок = "Помещение"
	//КонецЕсли;	
КонецПроцедуры
