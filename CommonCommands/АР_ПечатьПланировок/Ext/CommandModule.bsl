
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	//Если мИспользоватьМенеджерЛицензий Тогда
		//АР_ОбщиеПроцедуры.ПечатьПланировок(ПараметрКоманды);
	Параметры = Новый Структура;
	Параметры.Вставить("Ссылка",ПараметрКоманды);
	Форма = ПолучитьФорму("Обработка.ОтобразитьНаПлане.Форма",Параметры);
	Форма.Открыть();
	//Иначе
	//	Сообщить("Конфигурация <Аренда и управление недвижимостью> работает в демонстрационном режиме!");
	//КонецЕсли;

КонецПроцедуры
